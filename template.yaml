AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: ap.jeremy.ninja panel case builder (Fiber on Lambda)

Parameters:
  DomainName:
    Type: String
    Default: ap.jeremy.ninja
  CertificateArn:
    Type: String
  HostedZoneId:
    Type: String
  LwaClientId:
    Type: String
    Default: ""
    NoEcho: true
  LwaClientSecret:
    Type: String
    Default: ""
    NoEcho: true
  SessionSecret:
    Type: String
    Default: ""
    NoEcho: true

Globals:
  Function:
    Architectures: [arm64]

Resources:
  WebLogs:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: /aws/lambda/adafruit-pannel-web
      RetentionInDays: 7

  RenderLogs:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: /aws/lambda/adafruit-pannel-render
      RetentionInDays: 7

  Table:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: adafruit-pannel
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: pk
          AttributeType: S
        - AttributeName: sk
          AttributeType: S
      KeySchema:
        - AttributeName: pk
          KeyType: HASH
        - AttributeName: sk
          KeyType: RANGE

  HttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: $default
      Domain:
        DomainName: !Ref DomainName
        CertificateArn: !Ref CertificateArn
        EndpointConfiguration: REGIONAL
        Route53:
          HostedZoneId: !Ref HostedZoneId

  RenderFunction:
    Type: AWS::Serverless::Function
    DependsOn: RenderLogs
    Metadata:
      DockerTag: panel-render
      DockerContext: .
      Dockerfile: Dockerfile
    Properties:
      PackageType: Image
      FunctionName: adafruit-pannel-render
      Description: OpenSCAD tray+lid render (direct invoke, not API Gateway)
      MemorySize: 3538
      Timeout: 900
      EphemeralStorage:
        Size: 2048
      Architectures: [arm64]
      LoggingConfig:
        LogGroup: /aws/lambda/adafruit-pannel-render
      Environment:
        Variables:
          PANEL_ROLE: render
          BAMBU_S3_BUCKET: perq-export-public-759775734231
          JOBS_S3_PREFIX: dl/adafruit-pannel/bambu/jobs
          HOME: /tmp
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - s3:PutObject
              Resource: arn:aws:s3:::perq-export-public-759775734231/dl/adafruit-pannel/bambu/jobs/*

  WebFunction:
    Type: AWS::Serverless::Function
    DependsOn: WebLogs
    Metadata:
      BuildMethod: makefile
    Properties:
      FunctionName: adafruit-pannel-web
      Handler: bootstrap
      Runtime: provided.al2023
      Timeout: 60
      MemorySize: 1024
      CodeUri: .
      Description: Panel case builder (Fiber)
      Environment:
        Variables:
          TABLE: !Ref Table
          PUBLIC_BASE: !Sub https://${DomainName}
          BAMBU_S3_BUCKET: perq-export-public-759775734231
          BAMBU_S3_PREFIX: dl/adafruit-pannel/bambu
          JOBS_S3_PREFIX: dl/adafruit-pannel/bambu/jobs
          RENDER_FUNCTION_NAME: adafruit-pannel-render
          LWA_CLIENT_ID: !Ref LwaClientId
          LWA_CLIENT_SECRET: !Ref LwaClientSecret
          SESSION_SECRET: !Ref SessionSecret
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - dynamodb:GetItem
                - dynamodb:PutItem
                - dynamodb:UpdateItem
                - dynamodb:DeleteItem
                - dynamodb:Query
                - dynamodb:BatchGetItem
                - dynamodb:BatchWriteItem
                - dynamodb:DescribeTable
              Resource:
                - !GetAtt Table.Arn
                - !Sub "${Table.Arn}/index/*"
            - Effect: Allow
              Action:
                - s3:PutObject
                - s3:GetObject
              Resource:
                - arn:aws:s3:::perq-export-public-759775734231/dl/adafruit-pannel/bambu/*
                - arn:aws:s3:::perq-export-public-759775734231/dl/adafruit-pannel/bambu/jobs/*
            - Effect: Allow
              Action:
                - lambda:InvokeFunction
              Resource: !GetAtt RenderFunction.Arn
      Events:
        Root:
          Type: HttpApi
          Properties:
            ApiId: !Ref HttpApi
            Path: /
            Method: ANY
        Proxy:
          Type: HttpApi
          Properties:
            ApiId: !Ref HttpApi
            Path: /{proxy+}
            Method: ANY

Outputs:
  SiteUrl:
    Value: !Sub https://${DomainName}
  TableName:
    Value: !Ref Table
